---
description: Keep the graphify knowledge graph current and use it for architecture questions
alwaysApply: true
---

# Graphify Workflow

This project maintains a graphify knowledge graph at `graphify-out/`.

## Before architecture questions

- Read `graphify-out/GRAPH_REPORT.md` for god nodes and community structure
- If `graphify-out/wiki/index.md` exists, navigate it instead of reading raw files

## After code changes

When you modify source files in a session (`lib/`, `test/`, `supabase/`, etc.):

1. Run `graphify update .` from the repo root before marking work complete
2. AST-only — no API cost; updates `graphify-out/graph.json`, `graph.html`, and `GRAPH_REPORT.md`
3. Include updated `graphify-out/` artifacts in the same commit when the user asks for a commit

For doc/paper/image semantic extraction, use full graphify with `GEMINI_API_KEY` or `GOOGLE_API_KEY` when needed.
